name: vondo_shared
description: Shared tenant-safe primitives for Vondo customer and vendor apps.
version: 1.0.0
publish_to: none

environment:
  sdk: ^3.11.5

dependencies:
  flutter:
    sdk: flutter
  app_links: ^6.4.1
  firebase_core: ^3.15.1
  firebase_messaging: ^15.2.9

dev_dependencies:
  flutter_test:
    sdk: flutter
  lints: ^6.0.0
  test: ^1.25.0
